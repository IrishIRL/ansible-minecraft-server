---
  - name: Init
    hosts: all
    become: yes
    roles:
      - init
    tags:
      - init

  - name: Collect info about all VMs
    hosts: all
    tasks:
      - setup:

  - name: Dependencies
    hosts: all
    become: yes
    roles:
      - dependencies
    tags:
      - dependencies

  - name: Docker
    hosts: all
    become: yes
    roles:
      - iptables
      - docker
    tags:
      - docker

  - name: Minecraft
    hosts: minecraft_server
    become: yes
    roles:
      - minecraft
    tags:
      - minecraft

  - name: Ngrok
    hosts: minecraft_server
    become: yes
    roles:
      - ngrok
    tags:
      - ngrok

#  - name: Rsyslog influx logging
#    hosts: all
#    become: yes
#    roles:
#      - rsyslog
#      - node_exporter
#    tags:
#      - logging

